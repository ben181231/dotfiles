# gitignore.io CLI
function gi() { curl -L -s https://www.gitignore.io/api/$@ ;}

# hub CLI
eval "$(hub alias -s)"

# pyenv
if which pyenv > /dev/null; then eval "$(pyenv init -)"; fi

# fasd
eval "$(fasd --init auto)"

